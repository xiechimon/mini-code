# 02: 工具行摘要与四色

**What to build:** 工具调用从「原始 JSON 参数 + 800 字符结果倾倒」变为事件渲染里的新规则：调用行 = 工具名 + 关键参数摘要（read/write/edit 取 path；bash 取 command 截 60 字符；其他取紧凑 JSON 截 60）；结果成功 = 首行，多行追加「… 共 N 行」；失败 = 红色全文封顶 500 字符。四色角色落地：工具名青、参数暗灰、成功绿、失败红、轮首暗灰。去色模式下同一规则输出纯文本。

**Blocked by:** 01（事件渲染抽离与降级接线）

**Status:** resolved

- [x] 参数摘要规则单测：三类工具各一例 + 恰好 60 字符边界
- [x] 结果规则单测：单行 / 多行折叠计数 / 失败封顶 500 边界
- [x] 有色输出断言含 ANSI 角色色；去色输出同一规则纯文本
- [x] 真实 REPL 冒烟：一次工具调用可见新渲染（pty 实测）
- [x] mvn test 全绿
