# 04: 输入编辑器增强

**What to build:** 输入编辑器（Input Editor 面）三件套：❯ 提示符（有色模式下青色）；历史持久化——路径可注入（默认 `~/.mini-code/history`），重启后 ↑ 仍可翻上次输入；多行输入——行尾反斜杠续行拼接提交，粘贴多行整体进缓冲一次提交。Ctrl-C 弃行、Ctrl-D 退出语义保持。

**Blocked by:** 01（事件渲染抽离与降级接线，需其样式探测供 ❯ 着色）

**Status:** resolved

- [x] 历史持久化测试：注入临时目录路径，写入后新 reader 可翻到（@TempDir 先例）
- [x] 反斜杠续行测试：两行拼接为一次提交
- [x] 多行粘贴经 dumb/pty 终端验证整体进缓冲一次提交
- [x] ❯ 提示符可见，有色模式下青色
- [x] Ctrl-C 弃行 / Ctrl-D 退出语义保持（现有测试不回归）
- [x] mvn test 全绿
