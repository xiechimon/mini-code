# 04: 渲染重绘与 REPL 收尾

**What to build:** 流式的终端体验闭环：流式增量原样直出（宽度感知行数记账），首个片段到达时回退覆盖「思考中」占位行；MessageEnd 时光标上移+清除到底部，用 MarkdownRenderer 渲染版整段替换；aborted 终态追加「⏹ 已中断」标记；CLI 在流式期间注册 SIGINT 处理器映射到中断触发器（Ctrl-C → 优雅取消），流式结束注销恢复原语义（弃输入行/退出）；管道模式（非 tty）走非流式路径，输出无光标控制序列。pty 实测收口：真实网关流式逐字上屏、生成中 Ctrl-C 不退进程且 partial 保留、中断后继续对话正常。

**Blocked by:** 03（事件协议与中断语义）

**Status:** ready-for-agent

- [x] 流式增量直出+行数记账断言（EventRenderer 缝，宽度感知）
- [x] MessageEnd 重绘控制序列断言（光标上移+清除+渲染版替换）
- [x] 首片段覆盖占位行；aborted「⏹ 已中断」标记断言
- [x] SIGINT 注册/注销：流式期间映射到触发器，结束后恢复原语义（经注入触发器测试）
- [x] 管道模式：非流式、无光标控制序列断言
- [x] pty 冒烟：真实流式逐字上屏 + Ctrl-C 中断不退进程 + 中断后继续对话
- [x] mvn test 全绿
