# 02: 块级结构：代码块盒/列表/引用

**What to build:** 在已贯通的正文渲染管线上补齐块级结构：围栏代码块渲染为边框盒（上下边线+左竖线，内容暗灰，保持原始行不折行，特殊字符原样）；列表渲染为青色圆点加层级缩进（含嵌套列表）；引用块渲染为 `|` 前缀加暗灰斜体。去色模式下同一结构规则输出纯文本；混合嵌套（列表内代码块、引用内列表）不串样式。

**Blocked by:** 01（正文渲染管线贯通）

**Status:** ready-for-agent

- [ ] 代码块：边框盒/内容暗灰/不折行/原始行保留，有色与去色两态断言
- [ ] 列表：圆点青/层级缩进/嵌套列表，两态断言
- [ ] 引用：`|` 前缀/暗灰斜体/多段引用，两态断言
- [ ] 混合嵌套结构不串样式（列表内代码块、引用内列表）
- [ ] mvn test 全绿
