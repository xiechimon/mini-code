# 02: 结构性块闭合才定稿（围栏/缩进代码/表格）

**What to build:** 读 `.scratch/incremental-stream-render/spec.md`。在流式渲染组件植入两模式检测：结构性块（围栏代码块、缩进代码块、GFM 表格）在开放期**不逐字原位重绘**，而是累积，待闭合时**整块一次绘制成边框盒/对齐表格**——避免大面积闪烁与滚动擦除，且屏幕上不出现半截盒/半截表；段落、标题、列表项、引用等可流式块继续随增量原位重绘。检测规则：由缓冲起始结构判定（首行围栏/表格分隔线 → 结构性块），其余 → 可流式块。

**Blocked by:** 01（需要开放块定稿状态机）。

**Status:** ready-for-agent

- [ ] 围栏/缩进代码块开放期不逐字重绘，闭合时整块绘制为边框盒
- [ ] GFM 表格闭合时整块绘制为对齐表格
- [ ] 任何时刻屏幕不暴露裸 ```` ``` ```` / `|` 表格标记的残留
- [ ] 开放期结构性块不产生半截盒/半截表的闪烁残留
- [ ] 段落/标题/列表/引用仍随增量原位重绘（未被两模式检测破坏）
- [ ] 结构性块在中断（flush aborted）时能冲刷出已生成部分（如未闭合围栏）并保持「⏹ 已中断」语义
- [ ] 既有 markdown 渲染与 residue 测试零回归（本次仅加 seal-only 分支，不改渲染缝）
