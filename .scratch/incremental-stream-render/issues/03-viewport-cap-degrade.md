# 03: 开放块视图外 cap（超视口退化追加）

**What to build:** 读 `.scratch/incremental-stream-render/spec.md`。给流式渲染组件注入**视口高度**；当开放块的当前渲染行数超过视口高度时，停止 CUU+ED 原位重绘、改为**只追加**（增量文本直接往下走并进入回滚即终态），避免 CUU 追不到已滚出视口的内容而打烂屏幕。保持「已滚出内容即终态、无双份、无残留」的 invariant。视口高度由调用方注入（测试可注入小视口模拟），渲染组件不读环境。

**Blocked by:** 01（需要原位重绘状态机）。

**Status:** resolved

- [x] 注入视口高度；开放块渲染行数 ≤ 视口时正常原位重绘
- [x] 开放块渲染行数 > 视口时切换为只追加，不再 CUU+ED
- [x] 退化后最终屏幕仍无重复、无残留（含代码块/表格等结构性块）
- [x] 视口高度由调用方注入，渲染组件不读环境、不碰时钟
- [x] 仿真测试用小视口注入验证退化路径，原先行数 ≤ 视口的用例不回归
- [x] 与既有残渣/重复断言兼容（本 cap 不引入双份）
